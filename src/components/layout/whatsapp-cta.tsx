import { MessageCircle } from "lucide-react";
import { siteConfig } from "@/config/site";
export function WhatsAppCta() { const common = "fixed bottom-5 right-5 z-40 inline-flex min-h-11 items-center gap-2 border border-[color:var(--gold)] bg-[color:var(--background-dark)] px-3.5 text-xs font-bold uppercase tracking-[.1em] text-[color:var(--gold)] shadow-xl transition hover:bg-[color:var(--gold)] hover:text-[color:var(--background-dark)]"; if (!siteConfig.whatsappUrl) return <span className={`${common} cursor-not-allowed opacity-70`} aria-label="WhatsApp contact number will be announced" title="WhatsApp contact number will be announced"><MessageCircle size={18} strokeWidth={1.5} /><span className="hidden sm:inline">Chat on WhatsApp</span></span>; return <a href={siteConfig.whatsappUrl} target="_blank" rel="noreferrer" className={common} aria-label="Chat with Woodbay on WhatsApp"><MessageCircle size={18} strokeWidth={1.5} /><span className="hidden sm:inline">Chat on WhatsApp</span></a>; }

import { Button } from "@/components/ui/button";
import { updateDealerApplication } from "@/features/dealers/admin";

export function DealerApplicationEditForm({ application }: { application: { id: string; business_name: string; contact_person: string; phone: string; email: string | null; state: string; district: string; location: string; address: string | null; message: string | null } }) {
  return <form action={updateDealerApplication} className="mt-5 grid gap-4 sm:grid-cols-2"><input type="hidden" name="id" value={application.id} /><label>Business name<input required name="business_name" defaultValue={application.business_name} className="mt-1 min-h-11 w-full border px-3" /></label><label>Contact person<input required name="contact_person" defaultValue={application.contact_person} className="mt-1 min-h-11 w-full border px-3" /></label><label>Phone<input required name="phone" defaultValue={application.phone} className="mt-1 min-h-11 w-full border px-3" /></label><label>Email<input name="email" type="email" defaultValue={application.email ?? ""} className="mt-1 min-h-11 w-full border px-3" /></label><label>State<input required name="state" defaultValue={application.state} className="mt-1 min-h-11 w-full border px-3" /></label><label>District<input required name="district" defaultValue={application.district} className="mt-1 min-h-11 w-full border px-3" /></label><label>Area / location<input required name="location" defaultValue={application.location} className="mt-1 min-h-11 w-full border px-3" /></label><label className="sm:col-span-2">Address<input name="address" defaultValue={application.address ?? ""} className="mt-1 min-h-11 w-full border px-3" /></label><label className="sm:col-span-2">Message<textarea name="message" defaultValue={application.message ?? ""} rows={4} className="mt-1 w-full border p-3" /></label><Button className="sm:w-fit">Save application</Button></form>;
}

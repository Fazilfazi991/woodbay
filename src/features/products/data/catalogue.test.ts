import { describe, expect, it } from "vitest";
import { PAGE_SIZE, parseCatalogueParams, primaryImage } from "./catalogue";
describe("catalogue helpers", () => { it("parses safe URL-driven filters", () => expect(parseCatalogueParams({ q: "pantry", page: "2", sort: "name-desc", subcategory: "pantry-solutions" })).toEqual({ q: "pantry", page: 2, sort: "name-desc", subcategory: "pantry-solutions" })); it("falls back safely for invalid pagination", () => expect(parseCatalogueParams({ page: "-3" }).page).toBe(1)); it("prefers primary images", () => expect(primaryImage({ images: [{ storage_key: "first", alt_text: null, sort_order: 0, is_primary: false }, { storage_key: "primary", alt_text: null, sort_order: 3, is_primary: true }] } as never)?.storage_key).toBe("primary")); it("uses a scalable page size", () => expect(PAGE_SIZE).toBe(12)); });

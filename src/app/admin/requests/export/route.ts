import {NextResponse} from "next/server";import {exportRequests} from "@/features/furniture/requests-admin";export async function GET(r:Request){const u=new URL(r.url),tab=u.searchParams.get("tab")==="outlets"?"outlets":"factory";try{const csv=await exportRequests(tab,Object.fromEntries(u.searchParams));return new NextResponse(csv,{headers:{"Content-Type":"text/csv; charset=utf-8","Content-Disposition":`attachment; filename=woodbay-${tab==='factory'?'factory-visits':'outlet-enquiries'}.csv`}})}catch{return NextResponse.json({error:"Unauthorized"},{status:403})}}

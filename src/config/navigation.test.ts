import { describe, expect, it } from "vitest";
import { primaryNavigation } from "./navigation";
describe("public navigation", () => { it("uses local absolute links", () => { const links = primaryNavigation.flatMap((item) => [item.href, ...(item.children?.map((child) => child.href) ?? [])]); expect(links.every((href) => href.startsWith("/"))).toBe(true); }); it("includes the required grouped navigation", () => { expect(primaryNavigation.find((item) => item.label === "Products")?.children).toHaveLength(4); expect(primaryNavigation.find((item) => item.label === "Dealers")?.children).toHaveLength(2); }); });
